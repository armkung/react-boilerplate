import a from 'b'

const a = 'กa'
const b = `ข ${c => c}`
const bb = `${a} ขข ${c}`
const bbb = `${a} <div class="a">ขขข</div> ${c}`
const bbbb = `Email${isMonthly ? ' (ถ้ามี)' : ''}`
const c = {
  'd': 'ค',
  'คค': 'ค'
}
const d = <div>ง  { p }</div>
const dd = <div>{ q } { <div>ง</div> }  { p }</div>
const ddd = <div>ด { a ? 'ง':'ก' } ท { p } </div>
const dddd = <div>{() => <div>ง</div> } ท</div>